# White Collar Realty CRM — Bug Report

**Audited build:** commit `4dd8ba1` ("Round 11") — source-only review (the app is login-gated; no live clicking was possible, so reproduction steps are written from code paths and marked *(inferred from code)* where a click sequence is implied rather than observed).
**Audited against:** Master QA spec §10, `docs/QA-AUDIT-FINDINGS.md` (older commit `60fa393`), `docs/QA-FEEDBACK-2026-06-02.md` (Lalit's UAT buckets A–I).
**Date:** 2026-06-02
**Auditor voice:** QA tester + sales-ops manager. Every row cites a real file path; severity reflects sales-floor impact, not just code smell.

**Severity scale:** P0 Blocker · P1 Critical · P2 Major · P3 Minor · P4 Enhancement.
**Status values:** Open · Resolved (`4dd8ba1`) · Won't-fix (by design).

> Screenshots referenced in the spec are marked *(screenshot to be captured during live UAT)* — they cannot be produced from source.

---

## Bug table

| ID | Severity | Page | Role | Device | Steps | Expected | Actual | Recommendation | Status |
|----|----------|------|------|--------|-------|----------|--------|----------------|--------|
| **B-01** | **P0** | Leads import / dedup (Bucket A) | Manager/Admin | Both | Import a lead list that contains a phone number already present in the CRM *(inferred from code — import path)* | Duplicate phone is detected and merged/blocked; one client = one record | Per Lalit's UAT (Bucket A) duplicates are entering the pipeline. No phone-normalisation + unique-merge guard is evidenced on the lead-create/import path. Two agents can work the same client unknowingly | Add phone normalisation (E.164, strip spaces/`+`/leading zeros) + a hard dedup check on lead create AND CSV/sheet import; surface a "possible duplicate" merge prompt. This is the #1 trust blocker for rollout | **Resolved — groundwork (`40878ae`)** · read-only duplicate detection + non-blocking "possible duplicate" warning on /leads/new, role-scoped via `leadScopeWhere(me)` so it never discloses a peer's lead. Merge/block UX + historical-phone backfill deferred to Lalit's sign-off |
| **B-02** | **P1** | Call Records (`/calls`) | Agent | Both | Log in as an AGENT → open **Call Records** → tap any recent-call row | Agent sees ONLY their own calls + their own clients' details | Recent-calls list (`src/app/(app)/calls/page.tsx` lines 67–83) is `prisma.callLog.findMany({ orderBy:{startedAt:"desc"}, take:50 })` with **no `userId`/owner scope**. An agent sees the latest 50 **company-wide** calls; tapping a row opens `CallsClient`'s summary panel exposing peers' lead name, phone, email, budget, BANT status/reason, configuration, "who is client", follow-up + recent-call notes. The **QualityList** (lines 89–96, 245–277) leaks the same peer rows | Scope the list query: `const callScope = isAgent ? { userId: me.id } : {}` and spread into the `findMany` where-clause; apply identically to the `quality` map. The heatmap (lines 31–50) is already correctly agent-scoped — mirror it | **Resolved (`1f30647`)** · list query now `where: isAgent ? { userId: me.id } : {}`; the `quality` map reuses the same scoped result set |
| **B-03** | **P2** | Dashboard (`/dashboard`) | Agent | Both | Log in as an AGENT → read the KPI hero tiles (Total clients, New today, Hot leads, Calls today, Ready-to-close, Needs-you, follow-ups) | Each tile counts only the agent's **own** book | Tiles are scoped by `teamScope` (`forwardedTeam`), not `ownerId: me.id` (`src/app/(app)/dashboard/page.tsx` lines 49, 64–90, 96–120). An agent sees **whole-team** totals, overstating "their" numbers and revealing team volume. The `BY SALESPERSON` table and `spStats` are correctly hidden for agents (line 205, ~699), so this is the remaining gap | Introduce `const meScope = isAdminOrMgr ? teamScope : { ownerId: me.id }` and use it for the agent-facing count tiles; keep team scope only for leadership views. (Tracked as the Round-12 item) | **Resolved (`1f30647`)** · `meScope`/`meActWhere`/`meCallWhere` narrow agent KPI tiles to their own book; identical to team scope for ADMIN/MANAGER so leadership view is unchanged |
| **B-04** | **P2** | Dashboard (`/dashboard`) | All | Both | Read the WhatsApp tile while a team filter (Dubai/India) is applied | WhatsApp count respects the selected team like every other tile | `waToday = prisma.whatsAppMessage.count({ where:{ receivedAt:{gte:todayStart} } })` (line 70) ignores `teamScope`/`teamActWhere`. With a team filter active, the WA number is company-wide and inconsistent with the adjacent Calls/Connected tiles | Scope WhatsApp by team. If `WhatsAppMessage` has no direct team column, join through its lead (e.g. `{ lead: { forwardedTeam: view } }`) mirroring `teamCallWhere` | **Resolved (`1f30647`)** · WA tile now scopes through the lead relation (`meWaWhere`): agent → own leads, leadership → selected team |
| **B-05** | **P3** | Dashboard (`/dashboard`) | All | Both | Read the "📞 Calls (mo)" KPI tile | A tile labelled "(mo)/monthly" shows the month's calls | Tile renders `value={callsToday}` with `sub="today"` while the title says "Calls (mo)" (`src/app/(app)/dashboard/page.tsx` ~lines 636/644). `callsToday` is `startedAt: { gte: todayStart }` (line 68) — a **today** figure mislabelled as monthly. Confuses anyone reading the board | Either relabel the tile to "Calls (today)" or add a true month-to-date count and bind it. Pick one and make label + value + sub agree | **Resolved (`1f30647`)** · added a true month-to-date `callsThisMonth` count; tile now binds it with the `monthRangeLabel` sub |
| **B-06** | **P2** | Properties list (`/properties`) | Agent | Both | Open **Properties** → expand a project's "matching leads" | Agent sees only their own matching leads | **FIXED Round 11.** `leadScope = await leadScopeWhere(me)` is now passed to `bestLeadsForProject(p.id, 5, leadScope)` (`src/app/(app)/properties/page.tsx` line ~134/139); `bestLeadsForProject` spreads `...scope` into its where-clause (`src/lib/leadsForProject.ts` line 124). Agents no longer see peers' client names/budgets | Verify in live UAT *(screenshot to be captured during live UAT)* | **Resolved (`4dd8ba1`)** |
| **B-07** | **P2** | Property detail (`/properties/[id]`) | Agent | Both | Open a project → read "Matching leads" and "Active discussions" | Both lists scoped to the agent | **FIXED Round 11.** `matchingLeads` uses `bestLeadsForProject(id, 10, leadScope)` and `activeDiscussions` spreads `...leadScope` into the `lead.findMany` (`src/app/(app)/properties/[id]/page.tsx` lines 34–46) | Verify in live UAT | **Resolved (`4dd8ba1`)** |
| **B-08** | **P0→Resolved** | Pipeline (`/pipeline`) | Agent | Both | Log in as AGENT → open **Pipeline** | Kanban shows only the agent's leads | **FIXED Round 11.** `scope = await leadScopeWhere(me)`; board where-clause is `{ ...scope, status:{ in: ... } }`; `?owner=` honoured only `if (me.role !== "AGENT")` (`src/app/(app)/pipeline/page.tsx` lines ~42–45). Previously unscoped (audit P0-1) | Verify in live UAT | **Resolved (`4dd8ba1`)** |
| **B-09** | **P1→Resolved** | Team & Roles (`/team`) | Agent | Both | Log in as AGENT → navigate to `/team` directly | Agents cannot see team pipeline value / response-time leaderboard | **FIXED Round 11.** Page now calls `requireRole("ADMIN","MANAGER")` (line ~26) → agents are redirected to `/dashboard`. Nav link is also gated (see B-12) | Verify redirect in live UAT | **Resolved (`4dd8ba1`)** |
| **B-10** | **P1→Resolved** | Leads (`/leads`) | Agent | Both | Log in as AGENT → append `?owner=<peerId>` or `?source=` to the Leads URL | Agent cannot pivot the list onto a peer's book or filter by source | **FIXED Round 11.** `?owner=` honoured only when `me.role !== "AGENT"` (lines ~72–75); `?source=` gated likewise (line ~65); LOST hidden from agents (lines ~51–53); `showSource={me.role!=="AGENT"}` passed to filters/list | Verify in live UAT | **Resolved (`4dd8ba1`)** |
| **B-11** | **P1→Resolved** | Dashboard — BY SALESPERSON (`/dashboard`) | Agent | Both | Log in as AGENT → scroll dashboard | Agent does not see a peer-comparison salesperson table | **FIXED Round 11.** Section gated `{isAdminOrMgr && (...)}` (~line 699) and `spStatsRaw` is skipped for agents (~line 205). (Distinct from B-03, which is the KPI-tile scope still open) | Verify in live UAT | **Resolved (`4dd8ba1`)** |
| **B-12** | **P2→Resolved** | Navigation (`MobileShell`) | Agent | Both | Log in as AGENT → open the sidebar (desktop) and bottom/hamburger nav (mobile) | "Team & Roles" and "Awaiting Team" links are not shown to agents | **FIXED Round 11.** Both links carry `managerOrAdmin: true` (`src/components/MobileShell.tsx` lines ~48–51) and are filtered from desktop (line ~141) and mobile (line ~236) nav | Verify in live UAT | **Resolved (`4dd8ba1`)** |
| **B-13** | **P3** | Leads filter bar (`LeadFilters`) | Agent | Both | Log in as AGENT → open the owner dropdown in the Leads filter bar | Either no owner dropdown, or it does nothing | The owner `<select>` (`src/components/LeadFilters.tsx` lines ~73–77) still renders for **all** roles client-side. It is **inert** for agents server-side (B-10 ignores `?owner=`), but showing peers' names in the dropdown is a minor info leak + dead control | Hide the owner `<select>` when `!showSource`/agent (it already receives a role-derived flag for the source dropdown — reuse it) | **Resolved (`1f30647`)** · owner `<select>` now gated behind the same `showSource` (leadership-only) flag |
| **B-14** | **P2** | Loading / error states (global) | All | Both | Trigger a slow query or a server error on Dashboard/Leads/Reports *(inferred from code — no `loading.tsx`/`error.tsx` observed in route groups)* | Skeletons on load; a friendly retry on error | Audit P2-5 flagged sparse loading/error states; no `loading.tsx`/`error.tsx` boundaries were found in the `(app)` route group during review. On a slow mobile connection the screen looks frozen | Add `loading.tsx` (skeleton) and `error.tsx` (retry) to the `(app)` group and heavy routes (dashboard, leads, reports). Low risk, high perceived-quality payoff | **Resolved (`1f9f5f5`)** · added `(app)/error.tsx` (friendly retry), `(app)/loading.tsx` + `(app)/reports/loading.tsx` skeletons; dashboard/leads already had theirs |
| **B-15** | **P2** | List queries — N+1 / over-include | All | Both | Load Call Records / Leads / Dashboard feeds on a large dataset *(inferred from code)* | Lists paginate; nested includes are bounded | Audit P2-6 flagged N+1 / heavy includes. `/calls` fetches 50 rows each with `lead → owner` + nested `callLogs(take:5) → user` (`src/app/(app)/calls/page.tsx` lines 67–83) — heavy and unscoped (compounds B-02). Several lists lack pagination | Add pagination/`take` caps and trim nested includes to displayed fields; revisit after B-02 scoping (smaller result set) | **Resolved (`078b353`)** · `/calls` recent-calls converted `include`→`select` (drops the ~100-col Lead + nested full-CallLog×5 over-fetch; B-02 `where` scope preserved); `/pipeline` previously-unbounded query now `take:300` + trimmed relations; `/leads`, `/action-list`, `/dashboard` feeds trimmed `owner`/`lead`/`user` relations to rendered columns. Projection/`take` only — no scope/logic change. Forward-looking (prod ~45 leads) |
| **B-16** | **P2** | Automations / Workflows toggle | Admin/Manager | Both | Open the Automations/Workflows settings; observe each toggle's default | Toggle reflects real persisted state | Audit P2-4: automations are shown hardcoded ON regardless of backend state, so the UI can claim a workflow is running when it isn't | Bind toggles to persisted config; if a workflow isn't wired yet, label it "Coming soon" and disable it rather than showing a misleading ON | **Resolved (`e2658de`)** · round-robin badge reads `getRoundRobinEnabled()` (ON/OFF); auto-dedupe shown as non-interactive "Always on" (it's unconditionally wired in `ingestLead`) |
| **B-17** | **P1** | Lead detail — BANT / qualification (Bucket B) | All | Both | Open a lead → try to record full BANT (Budget/Authority/Need/Timeline) and the client's real situation | BANT is structured, required at the right stage, and visible at a glance | Per Lalit (Bucket B) the lead-detail BANT capture is incomplete for how the team actually qualifies. Fields exist (`bantStatus`, `bantReason`, `whoIsClient`, budget, configuration in `calls/page.tsx` row mapping) but the workspace is a dense ~20-card page (`src/app/(app)/leads/[id]/page.tsx`) and the qualification flow needs tightening | Define the required BANT fields per stage with the team; make them prominent on lead detail; gate stage advancement on BANT completeness. Co-design with Lalit | **Open** |
| **B-18** | **P3** | Dashboard / Reports labels (Bucket C/D) | All | Both | Read dashboard tile labels and report headings | Labels match how the sales team talks (calls today vs month, connect rate, source) | Bucket C/D feedback: several labels are ambiguous (see B-05). Reports best-time-to-call IST formatting was corrected (Bucket D) but a label pass across dashboard + reports is still wanted | Do a labelling pass with Lalit; ensure every metric states its time window and scope (you vs team) | **Open** |
| **B-19** | **P4** | AI scoring — cold/warm/hot (Bucket E) | All | Both | Review how leads are auto-scored HOT/WARM/COLD and whether agents trust it | Scoring is explainable and matches manager intuition | Bucket E: the AI cold/warm/hot model needs validation against real outcomes; `aiScore` + `aiScoreValue` exist but the rationale isn't surfaced to build trust | Surface the "why" behind each score (top contributing signals); review thresholds with Lalit after a week of live data | **Open** |
| **B-20** | **P4** | Voice / motivation surface (Bucket H) | All | Both | Look for the motivational / team-mood + voice features Lalit asked about | Present and tasteful | Bucket H: voice/motivation features are partially specced (`docs/SPEC-smart-cma-and-voice.md`, team-mood) but not validated for tone/usefulness | Treat as enhancement; pilot with one team before enabling for all | **Open** |

---

## Immediate fix priority list

> **Round-12 update (deployed `40878ae`, 2026-06-03):** items 1–7 below are now **Resolved** — B-01 dedup *groundwork* (read-only detection + non-blocking, role-scoped warning; merge/block + phone backfill deferred to Lalit), plus B-02 (`/calls` leak), B-03 (agent KPI scope), B-04 (WA team scope), B-05 (Calls-mo label), B-13 (owner dropdown) and B-16 (automations state), and B-14 loading/error boundaries. The Status column above lists the resolving commit per item. **Still open:** B-15 (P2 perf/N+1 — low urgency: 45 leads in prod today), B-17 (P1 *product* — BANT depth, needs Lalit co-design), B-18 (P3 label pass, needs Lalit), B-19 (P4 AI-score explainability — in progress), B-20 (P4 voice/motivation pilot, needs Lalit).

Fix **before** full team rollout (these break trust or leak data between peers):

1. **B-01 (P0) — Lead dedup/merge.** The pipeline cannot be trusted while duplicate clients enter it. This is Lalit's stated #1 blocker (Bucket A). Highest priority.
2. **B-02 (P1) — `/calls` recent-calls list + QualityList agent scoping.** A real, currently-open agent↔agent leak: agents see the latest 50 company-wide calls and every peer client's BANT/budget/notes. One-line scope fix mirroring the already-fixed heatmap. **This survived Round 11 and the original audit wrongly described `/calls` as agent-scoped — re-verify.**
3. **B-03 (P2) — Dashboard agent KPI-tile scope.** Agents see team-wide totals on their own dashboard. Round-12 item; ship with B-02.

Fix **soon** (correctness/clarity, not leaks):

4. **B-04 (P2)** WhatsApp tile ignores team filter.
5. **B-05 (P3)** "Calls (mo)" mislabel (today vs month).
6. **B-16 (P2)** Automations hardcoded ON (misleading state).
7. **B-13 (P3)** Inert owner dropdown visible to agents.

Can wait / co-design with Lalit (not rollout blockers):

8. **B-17 (P1 product)** BANT/qualification depth (Bucket B) — needs Lalit's input; structural, not a quick fix. **← only remaining substantive item; needs Lalit's co-design.**
9. ~~**B-14, B-15 (P2)** loading/error states + pagination/N+1 hardening.~~ **Both Resolved** (B-14 `1f9f5f5`, B-15 `078b353`).
10. **B-18 (P3, open)** label pass + ~~**B-19 (P4)** AI-score explainability~~ **(Resolved `0b9b5b0`)** + **B-20 (P4, open)** voice/motivation pilot. B-18/B-20 both need Lalit's input.

**By design — do NOT "fix":** the Sales Floor Live Feed and leaderboards on the dashboard are intentionally peer-visible per spec §12.2 (team-scoped, not a leak).
